let user = {
    name: prompt(``).trim(),
    age: +prompt(``).trim(),
    birthday: prompt(``).trim(),
    status: prompt(``).trim(),
}
console.log(user);

let a = prompt(``);
let b = prompt(``);
let c = prompt(``);
let d = prompt(``);

// let user = {
//   name: null,
//   age: null,
//   birthday: null,
//   status: null,
// };

// if (isNaN(a) && isNaN(b) && isNaN(c) && isNaN(d)) {
//     user.name = a;
//     user.age = b;
//     user.birthday = d;
//     user.status = a;
// }
// console.log(user);